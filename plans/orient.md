# Plan helper: orient

Use when user is lost mid-project.

Steps:
1. Read `GRILLME.md`
2. `git log --oneline -10`
3. `git status`
4. `git diff HEAD~1` (if commits exist)
5. Read top-level config files
6. Summarize back in exactly 3 bullets: what, where, half-done
